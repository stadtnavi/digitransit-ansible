[Unit]
Description=Build mbtiles
Requires=docker.service
After=docker.service

[Service]
WorkingDirectory=/var/tileserver
ExecStart=/var/tileserver/build-mbtiles

[Install]
WantedBy=multi-user.target
